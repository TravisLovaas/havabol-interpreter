package havabol.storage;

public class Numeric
{

	int integerValue;
	double doubleValue;
	String strValue;		//display value
	int type; 				//Integer, Float
	
	public Numeric()
	{
		//we'll work on this in the future
		//But here, we have a numeric class!
		//if type is an int, if includes decimal
		//remove decimals
		//if type is a double, if doesn't include decimal
		//add decimal
		//just take a result expression and convert it to its object type
		
	}

}
